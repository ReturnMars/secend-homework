# High-Performance Data Cleaner & ETL Engine

这是一个基于 **Go + React + PostgreSQL** 构建的高性能数据清洗平台。本项目专为在极端硬件资源限制下（如 2核2G 内存）处理大规模数据集（GB 级文件 / 千万级行数）而设计。

## 🚀 核心亮点：2G 内存挑战千万级数据

本项目在架构设计上通过多项极致优化，打破了 Web 应用处理大文件的内存瓶颈：

- **极致内存控制**：采用 **Row-based Streaming Iterator**（行级流式迭代器），在处理 2G 以上的大文件时，系统常驻内存占用极低。
- **高性能写入引擎**：
  - **UNLOGGED Table**：通过禁用后端表的 WAL 日志记录，将数据库写入吞吐量提升至极致。
  - **Index Rebuild Strategy**：入库期间动态卸载索引，通过顺序 I/O 写入后再利用内存排序重建，效率远超实时索引维护。
  - **Batch Buffering**：精心调优的 4000 行级批次写入，平衡了 I/O 频率与内存开销。
- **并发流水线 (Pipeline)**：利用 Go 协程实现的“生产者-多工清洗器-批量持久化”模型，充分榨干 CPU 多核性能。

---

## 🛠️ 技术栈

### 后端 (Backend - Go API)
- **Runtime**: Go 1.23
- **ORM**: GORM (针对 PostgreSQL 17 进行深度优化)
- **Concurrency**: Worker Pool 模式处理异步数据清洗任务
- **DB Migration**: golang-migrate
- **Validation**: 包含手机号格式修复、地址多级提取、日期标准化等内置清洗算法。

### 前端 (Frontend - React)
- **Framework**: Vite + React + TypeScript
- **State Management**: React Hooks + 异步任务监听
- **UI Architecture**: 自定义的任务进度调度中心 (`TaskManager`)，支持大文件上传进度反馈与处理状态实时同步。

---

## 🏗️ 项目架构

```mermaid
graph TD
    Client[React Frontend] -- 1. Stream Upload --> API[Go Backend Service]
    API -- 2. Row Iterator --> Cleaner[Parallel Worker Pool]
    Cleaner -- 3. Cleaned Data --> Buffer[Memory Batch Buffer]
    Buffer -- 4. Bulk Insert --> DB[(PostgreSQL 17)]
    DB -- 5. Optimization --> Tune[Unlogged Table / Index Drop-Rebuild]