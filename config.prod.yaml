# ==============================================================================
# 🚀 CSV Cleaner 全局配置 (YAML 版本)
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. 数据库配置 (Postgres)
# ------------------------------------------------------------------------------
database:
  user: "postgres"
  password: "123456"
  name: "etl_db"
  host: "db" # Docker Compose 内部服务名
  port: 5432
  ssl_mode: "disable"
  timezone: "Asia/Shanghai"
  batch_insert_concurrency: 2 # 写入并发数，默认 2，高性能机可调大

# ------------------------------------------------------------------------------
# 2. 后端服务配置 (Go / Gin)
# ------------------------------------------------------------------------------
server:
  port: 8080
  mode: "release" # release, debug, test
  jwt_secret: "generate_a_random_long_string_here"
  upload_dir: "./uploads"

# ------------------------------------------------------------------------------
# 3. 前端配置 (Vite)
# ------------------------------------------------------------------------------
frontend:
  api_base_url: "/api"
  external_port: 27182 # 生产环境访问端口，如 80 或 443

# ------------------------------------------------------------------------------
# 4. 远程部署配置 (Deployer Tool 使用)
# ------------------------------------------------------------------------------
deploy:
  remote_host: "47.109.195.0"
  remote_user: "root"
  ssh_key_path: "~/.ssh/id_rsa"
  remote_password: "Aioreturn@123" # 填写您的服务器密码
  deploy_path: "/root/apps/etl-tool"
  # 资源限制策略
  limits:
    memory_db: "400M"
    memory_backend: "1.2G"
