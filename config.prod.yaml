# ==============================================================================
# 🚀 CSV Cleaner 全局配置 (YAML 版本)
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. 数据库配置 (Postgres)
# ------------------------------------------------------------------------------
database:
  user: "postgres"
  password: "123456"
  name: "etl_db"
  host: "db" # Docker Compose 内部服务名
  port: 5432
  ssl_mode: "disable"
  timezone: "Asia/Shanghai"
  # PostgreSQL 性能参数（根据服务器配置调整）
  shared_buffers: "64MB" # 低配: 64MB, 高配: 256MB
  work_mem: "4MB" # 低配: 4MB, 高配: 16MB
  maintenance_work_mem: "32MB" # 低配: 32MB, 高配: 128MB
  effective_cache_size: "128MB" # 低配: 128MB, 高配: 512MB
  max_connections: 20 # 低配: 20, 高配: 100

# ------------------------------------------------------------------------------
# 2. 后端服务配置 (Go / Gin)
# ------------------------------------------------------------------------------
server:
  port: 8080
  mode: "release" # release, debug, test
  jwt_secret: "generate_a_random_long_string_here"
  upload_dir: "./uploads"

# ------------------------------------------------------------------------------
# 3. 前端配置 (Vite)
# ------------------------------------------------------------------------------
frontend:
  api_base_url: "/api"
  external_port: 27182 # 生产环境访问端口，如 80 或 443

# ------------------------------------------------------------------------------
# 4. 远程部署配置 (Deployer Tool 使用)
# ------------------------------------------------------------------------------
deploy:
  remote_host: "47.109.195.0"
  remote_user: "root"
  ssh_key_path: "~/.ssh/id_rsa"
  remote_password: "Aioreturn@123" # 填写您的服务器密码
  deploy_path: "/root/apps/etl-tool"
  # 资源限制策略（根据服务器配置调整）
  limits:
    memory_db: "256M" # 低配: 256M, 高配: 1G
    memory_backend: "512M" # 低配: 512M, 高配: 2G
    memory_frontend: "64M" # Nginx 静态服务
