# ==============================================================================
# 🚀 CSV Cleaner 开发环境配置 (Dev)
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. 数据库配置 (Postgres)
# ------------------------------------------------------------------------------
database:
  user: "postgres"
  password: "123456"
  name: "csv_cleaner"
  host: "localhost"
  port: 5436
  ssl_mode: "disable"
  timezone: "Asia/Shanghai"
  batch_insert_concurrency: 4 # 写入并发数，默认 2，高性能机可调大

# ------------------------------------------------------------------------------
# 2. 后端服务配置 (Go / Gin)
# ------------------------------------------------------------------------------
server:
  port: 8080
  mode: "debug"
  jwt_secret: "dev_secret_key_for_local_testing"
  upload_dir: "./uploads"

# ------------------------------------------------------------------------------
# 3. 前端配置 (Vite)
# ------------------------------------------------------------------------------
frontend:
  api_base_url: "/api"
  external_port: 5173 # 生产环境访问端口，如 80 或 443
