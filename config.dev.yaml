# ==============================================================================
# 🚀 CSV Cleaner 开发环境配置 (Dev)
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. 数据库配置 (Postgres)
# ------------------------------------------------------------------------------
database:
  user: "postgres"
  password: "123456"
  name: "csv_cleaner"
  host: "localhost"
  port: 5436
  ssl_mode: "disable"
  timezone: "Asia/Shanghai"
  # PostgreSQL 性能参数（高配开发机）
  shared_buffers: "256MB"
  work_mem: "16MB"
  maintenance_work_mem: "128MB"
  effective_cache_size: "512MB"
  max_connections: 100

# ------------------------------------------------------------------------------
# 2. 后端服务配置 (Go / Gin)
# ------------------------------------------------------------------------------
server:
  port: 8080
  mode: "debug"
  jwt_secret: "dev_secret_key_for_local_testing"
  upload_dir: "./uploads"

# ------------------------------------------------------------------------------
# 3. 前端配置 (Vite)
# ------------------------------------------------------------------------------
frontend:
  api_base_url: "/api"
  external_port: 5173 # 开发环境 Vite 端口
